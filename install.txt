https://pytorch.org/get-started/previous-versions/, for me:
pip install torch==2.1.0 torchvision==0.16.0 torchaudio==2.1.0 --index-url https://download.pytorch.org/whl/cu121 
*Note*: cu121 = cuda 12.1, cu118 = cuda 11.8 get this from running "nvcc --version". 

Add "export LIBTORCH_USE_PYTORCH=1" to .bashrc .

Run "source .bashrc"

Run "sudo apt install libssl-dev" (may need to run "sudo apt update")
Run "sudo apt install pkg-config "